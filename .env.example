# Local dev only. CI reads these from GitHub repo secrets / vars, NOT from this file.
# Copy to .env and fill for local runs. .env is gitignored.
GITHUB_TOKEN=
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
SKIP_CLEANUP=
